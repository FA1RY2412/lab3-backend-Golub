
# Copy to .env and adjust if needed
POSTGRES_USER=labuser
POSTGRES_PASSWORD=labpass
POSTGRES_DB=expenses

# SQLAlchemy style URI for the Flask app:
# Use the container hostname 'db' when running via docker-compose
DATABASE_URL=postgresql+psycopg2://labuser:labpass@db:5432/expenses
